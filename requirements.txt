# FastAPI框架（核心Web服务依赖）
fastapi==0.111.0

# Uvicorn（ASGI服务器，用于运行FastAPI应用）
uvicorn==0.30.1

# OpenAI 兼容 SDK（用于调用火山方舟 / DeepSeek / 其他 OpenAI 协议服务）
# 火山方舟官方完全支持 OpenAI 兼容协议，通过 base_url 切换服务商
openai>=1.0.0

# Requests（备用 HTTP 客户端）
requests==2.32.3

# .env 文件解析
python-dotenv>=1.0.0
